export default function Loading() {
  return (
    <div
      style={{
        minHeight: "80vh",
        // minWidth: "100vw",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <p>Loading...</p>
    </div>
  );
}
